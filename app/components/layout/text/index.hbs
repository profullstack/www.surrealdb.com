<layout-text ...attributes page={{route-name}}>
	<content route={{route-name}}>
		{{#if @title}}<h2>{{@title}}</h2>{{/if}}
		{{#if @text}}<p>{{@text}}</p>{{/if}}
		{{yield}}
	</content>
</layout-text>